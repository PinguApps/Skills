interface:
  display_name: "Finish PR"
  short_description: "Finish current PR through Codex approval"
  default_prompt: "Use $finish-pr to resolve conflicts and failed CI on the current branch's PR, action review feedback without duplicating agent replies awaiting a response, and continue until Codex gives the current HEAD a thumbs-up."
